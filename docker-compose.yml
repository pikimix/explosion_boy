services:
  server:
    build: .
    ports:
      - "${HOST_PORT:-9000}:${SERVER_PORT:-9000}"
    environment:
      SERVER_HOST: ${SERVER_HOST:-0.0.0.0}
      SERVER_PORT: ${SERVER_PORT:-9000}
      SERVER_BACKEND: ${SERVER_BACKEND:-tcp}
    restart: unless-stopped
