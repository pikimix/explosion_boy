[project]
name = "explosion-boy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "arcade>=3.0",
    "msgpack>=1.1",
    "py-spy>=0.4.2",
    "pymunk>=6.0",
]

[dependency-groups]
dev = [
    "py>=1.11.0",
    "pylint>=4.0.6",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
